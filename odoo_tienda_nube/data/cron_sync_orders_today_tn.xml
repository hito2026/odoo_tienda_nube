<odoo>
    <data noupdate="1">

        <record id="cron_sync_orders_today_tn" model="ir.cron">
            <field name="name">Tienda Nube: Sincronizar órdenes del día</field>
            <field name="model_id" ref="base.model_res_company"/>
            <field name="state">code</field>
            <field name="code">
if not env['res.company']._tn_sync_disabled():
    for company in env['res.company'].search([('tiendanube_access_token', '!=', False), ('tiendanube_id', '!=', False)]):
        company.cron_sync_orders_today_tn()
            </field>
            <field name="interval_number">2</field>
            <field name="interval_type">hours</field>
            <field name="active">True</field>
        </record>

    </data>
</odoo>
