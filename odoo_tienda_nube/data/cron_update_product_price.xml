<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="ir_cron_update_product_price_tn" model="ir.cron">
            <field name="name">Actualizar Precios en Tienda Nube</field>
            <field name="model_id" ref="base.model_res_company"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="state">code</field>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="code">
if model._tn_sync_disabled():
  companies = model.browse()
else:
  companies = model.search([('tiendanube_access_token','!=',False)])
for company in companies:
  if company.tn_config_update_product_price_cron:
    products = env['product.template'].search([('id_tn', '!=', False)])
    if len(products):
      company.update_product_price_tn(products)</field>
        </record>
    </data>
</odoo>